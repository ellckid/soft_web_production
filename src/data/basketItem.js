export default class BasketItem {

  constructor(id, title, color,url,price,count) {
      this.id = id;
      this.title = title;
      this.color = color;
      this.url = url;
      this.price = price;
      this.count = count;
  }
}